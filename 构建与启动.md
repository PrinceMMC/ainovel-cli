## 构建与启动
### 构建命令
在项目根目录 d:\Niko\ainovel-cli\ainovel-cli 下执行：

```
go build -o ainovel-cli.exe ./cmd/ainovel-cli/
```
### 产物位置
构建产物就落在 项目根目录 ：

```
d:\Niko\ainovel-cli\ainovel-cli\ainovel-cli.exe
```
### 启动
```
.\ainovel-cli.exe
```
首次启动会进入 TUI 引导配置（设置 API Key / Provider / 模型等），之后就会进入主界面。